# Site kök URL'i — canonical, hreflang, sitemap ve schema.org için.
NEXT_PUBLIC_SITE_URL=https://hibrid360.com

# Supabase (docs/supabase-schema.sql). Tanımlı değilse içerik sorguları boş
# döner ve sayfalar "hazırlanıyor" boş durumunu gösterir — build kırılmaz.
# Yalnızca publishable (anon) anahtar kullanılır; RLS politikaları
# published=true satırları herkese açar, form tabloları yalnızca insert alır.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
