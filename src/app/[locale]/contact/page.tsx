import { PageStub } from "@/components/PageStub";

// TODO: brief-rev12.md Bölüm 13 — Contact (form + WhatsApp + Brief Builder 18.8 + randevu)
export default function ContactPage() {
  return <PageStub title="CONTACT" />;
}
